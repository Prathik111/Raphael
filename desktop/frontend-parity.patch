diff -ruN a/desktop/public/raphael-stage.html b/desktop/public/raphael-stage.html
--- a/desktop/public/raphael-stage.html	2026-09-17 09:10:57.877673365 +0000
+++ b/desktop/public/raphael-stage.html	2026-09-17 09:10:57.881590135 +0000
@@ -1697,16 +1697,7 @@
 
 </style>
 
-<style id="tauri-stage-override">
-  /* Raphael Tauri stage: preserve the prototype's renderer and animation,
-     but suppress prototype chrome. React owns the actual chat UI. */
-  html, body { width:100%!important; height:100%!important; margin:0!important; padding:0!important; overflow:hidden!important; background:#030b08!important; }
-  body > *:not(#canvas-wrap):not(#core-pole):not(#caption-box) { display:none!important; visibility:hidden!important; opacity:0!important; pointer-events:none!important; }
-  #caption-box { display:block!important; visibility:visible!important; pointer-events:auto!important; z-index:20!important; }
-  #canvas-wrap { display:block!important; visibility:visible!important; opacity:1!important; position:fixed!important; inset:0!important; pointer-events:none!important; }
-  #canvas-wrap canvas { display:block!important; width:100%!important; height:100%!important; }
-  #core-pole { display:block!important; visibility:visible!important; opacity:.55!important; pointer-events:none!important; }
-</style>
+
 
 </head>
 <body class="startup-snap startup-active startup-intro">
@@ -4703,7 +4694,7 @@
   // below actually gets used; captionEnabled (the "Show caption text"
   // checkbox) can turn either one off entirely. Both declared here so
   // showCaption() can reference them regardless of definition order.
-  let captionMode = RAPHAEL_TAURI_STAGE ? 'box' : 'bar';
+  let captionMode = 'bar';
   const captionBox = document.getElementById('caption-box');
   const captionTextEl = document.getElementById('caption-text');
   const cinematicCaption = document.getElementById('cinematic-caption');
@@ -5066,7 +5057,7 @@
   const SFX_GAIN = {
     overlayAppear: 1.2,      // Krazaa: 20% up, it was too quiet
   };
-  let sfxEnabled = !RAPHAEL_TAURI_STAGE;
+  let sfxEnabled = true;
   let sfxVolume = 0.7;
   const sfxPool = {};
 
@@ -5805,9 +5796,77 @@
                     (r && (r.stack || r.message)) || r);
   });
 
+  let raphaelBackend = { base: 'http://127.0.0.1:8765', token: null };
+  let raphaelBackendReady = false;
+
+  async function raphaelRequest(path, init = {}) {
+    const headers = Object.assign({ 'Content-Type': 'application/json' }, init.headers || {});
+    if (raphaelBackend.token) headers.Authorization = 'Bearer ' + raphaelBackend.token;
+    const res = await fetch(raphaelBackend.base + path, Object.assign({}, init, { headers }));
+    if (!res.ok) {
+      const detail = await res.text();
+      throw new Error(res.status + ': ' + detail);
+    }
+    return res.json();
+  }
+
+  async function sendChatMessageViaRest(text) {
+    if (!raphaelBackendReady || !raphaelBackend.token) {
+      if (wsStatusEl) wsStatusEl.textContent = 'AI: waiting for backend';
+      return;
+    }
+    audioCtx.resume();
+    replyBuffer = '';
+    cinematicRenderedText = '';
+    ensureActiveTopic();
+    const topic = currentTopic();
+    if (topic.title === 'New chat') {
+      topic.title = text.length > 40 ? text.slice(0, 40) + '…' : text;
+    }
+    topic.messages.push({ role: 'user', text });
+    topic.updated = Date.now();
+    renderTopicList();
+    renderHistory();
+    saveChats();
+    aiThinking = true;
+    if (thinkHarder) setThinkingBusy(true);
+    chatInput.value = '';
+    try {
+      const task = await raphaelRequest('/tasks', { method: 'POST', body: JSON.stringify({ goal: text }) });
+      let current = task;
+      for (let i = 0; i < 180; i += 1) {
+        await new Promise((resolve) => setTimeout(resolve, 500));
+        current = await raphaelRequest('/tasks/' + task.task_id);
+        if (current.completed || current.state === 'FAILED' || current.state === 'CANCELLED') break;
+      }
+      const result = await raphaelRequest('/tasks/' + task.task_id + '/result');
+      const reply = result.reply || result.summary || result.error || 'Task completed without a response.';
+      topic.messages.push({ role: 'assistant', text: reply });
+      topic.updated = Date.now();
+      renderTopicList();
+      renderHistory();
+      saveChats();
+      aiThinking = false;
+      if (window.__endThinkingBusy) window.__endThinkingBusy();
+      if (wsStatusEl) wsStatusEl.textContent = result.status === 'COMPLETED' ? 'AI: ready' : ('AI: ' + String(result.status || current.state || 'done').toLowerCase());
+      showCaption(reply);
+    } catch (err) {
+      aiThinking = false;
+      if (window.__endThinkingBusy) window.__endThinkingBusy();
+      if (wsStatusEl) wsStatusEl.textContent = 'AI: error';
+      showCaption('[Error] ' + (err && err.message ? err.message : String(err)));
+    }
+    clearAttachment();
+  }
+
   function sendChatMessage() {
     const text = chatInput.value.trim();
-    if (!text || !ws || ws.readyState !== WebSocket.OPEN) return;
+    if (!text) return;
+    if (RAPHAEL_TAURI_STAGE) {
+      void sendChatMessageViaRest(text);
+      return;
+    }
+    if (!ws || ws.readyState !== WebSocket.OPEN) return;
     audioCtx.resume();
     replyBuffer = '';
     cinematicRenderedText = ''; // next showCaption() starts a fresh typed-out caption
@@ -7194,41 +7253,29 @@
     renderer.clear();
     renderer.render(warpScene, warpCamera);
 
-    // Pass 2: the model + effects. The browser version uses the original
-    // supersampled foreground render target and composite shader because
-    // that is part of the reference visual's anti-aliasing pipeline.
-    //
-    // Tauri/WebView2 gets a safer path: render the background first, then
-    // draw the actual model scene directly into the default framebuffer.
-    // The broken screenshot that motivated this integration showed the
-    // warp background rendering correctly while the entire foreground
-    // model disappeared. That symptom isolates the failure to the
-    // off-screen foreground-target/composite leg rather than the scene,
-    // camera, or Three.js initialization. Direct compositing keeps the
-    // reference scene and animation intact while avoiding that fragile
-    // render-target hop in the desktop WebView.
-    const renderStart = performance.now();
+    // Pass 2: the model + effects into the supersampled target (filtered
+    // back down at composite time - that downsample is what anti-aliases
+    // the lines), transparent where empty so the background shows
+    // through the gaps.
+    renderer.setRenderTarget(fgTarget);
+    renderer.setClearColor(0x000000, 0);
+    renderer.clear(true, true, true);
+    renderer.render(scene, camera);
+
+    // Composite both onto the screen. The RGB-channel-split offset is a
+    // fixed base amount now - it used to randomly spike stronger every so
+    // often, which read as the screen itself glitching rather than a
+    // steady lens-style chromatic-aberration look. Its actual on-screen
+    // strength still isn't uniform: cornerVignetteMask() in the shader
+    // scales it up toward the edges/corners and down to ~0 at center.
     renderer.setRenderTarget(null);
     renderer.setClearColor(0x000000, 1);
     renderer.clear();
     bgComposite.material.uniforms.uRgbOffset.value.set(0.009, 0.0036);
+    // fgComposite's offset stays at (0,0) - see note above, never touched here.
+    const renderStart = performance.now();
     renderer.render(bgComposite.scene, warpCamera);
-
-    if (RAPHAEL_TAURI_STAGE) {
-      // Keep the background, but only reset depth before painting the 3D
-      // model. autoClear stays false so the background remains untouched.
-      renderer.clearDepth();
-      renderer.render(scene, camera);
-    } else {
-      // Reference-browser path: retain the original supersampled
-      // foreground render target + composite shader.
-      renderer.setRenderTarget(fgTarget);
-      renderer.setClearColor(0x000000, 0);
-      renderer.clear(true, true, true);
-      renderer.render(scene, camera);
-      renderer.setRenderTarget(null);
-      renderer.render(fgComposite.scene, warpCamera);
-    }
+    renderer.render(fgComposite.scene, warpCamera);
     const renderMs = performance.now() - renderStart;
 
     // Small rolling governor. It changes quality only after ~1s of evidence,
@@ -9467,7 +9514,19 @@
   window.addEventListener('message', (event) => {
     const msg = event && event.data;
     if (!msg || msg.source !== 'raphael-react') return;
-    if (msg.type === 'state') {
+    if (msg.type === 'backend') {
+      raphaelBackend = { base: String(msg.base || 'http://127.0.0.1:8765'), token: msg.token ? String(msg.token) : null };
+      raphaelBackendReady = !!raphaelBackend.token;
+      if (wsStatusEl) wsStatusEl.textContent = raphaelBackendReady ? 'AI: ready' : 'AI: waiting for backend';
+    } else if (msg.type === 'new-chat') {
+      const topic = { id: Date.now(), title: 'New chat', messages: [], created: Date.now(), updated: Date.now(), summary: '' };
+      chatTopics.push(topic);
+      activeTopicId = topic.id;
+      setChatWorkspaceView(false);
+      renderTopicList();
+      renderHistory();
+      saveChats();
+    } else if (msg.type === 'state') {
       aiThinking = !!msg.thinking;
       if (!aiThinking && typeof setThinkingBusy === 'function') setThinkingBusy(false);
       if (aiThinking && typeof setThinkingBusy === 'function') setThinkingBusy(true);
